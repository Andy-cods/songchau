'use client';

// Tổng quan now renders the daily report — per user request 2026-05-05.
// Old chart-heavy dashboard preserved at /dashboard/charts.
export { default } from '@/app/(dashboard)/reports/daily/page';
