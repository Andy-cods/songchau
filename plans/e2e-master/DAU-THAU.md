# Test case E2E — Đấu thầu NCC + cổng vendor (CHỈ DELTA)

> **Phạm vi**: file này **CHỈ DELTA sau 30/06**. Phần lõi (mở vòng, mời NCC, Q&A, sealed-bid, maker-checker, ma trận giá, NCC nộp báo giá, merge-approved) đã có **118 ca TC-IND/TC-CMB** tại `<workspace>/plans/bidding-e2e-test-plan/` (chú ý: ở gốc workspace, NGOÀI songchau-erp/) **+ 276 ca** `<workspace>/plans/commercial-bidding/E2E_TEST_CASES.md` (cũng gốc workspace, NGOÀI songchau-erp/). File này KHÔNG lặp lại 2 bộ đó — chỉ viết ca cho phần mới phát sinh sau khi 2 bộ trên chốt (module Xếp hạng NCC / Vendor Scorecard / Items History / Smart-Award).

Phạm vi: CHỈ các tính năng **chưa được phủ** bởi bộ 118 ca đã có (`<workspace>/plans/bidding-e2e-test-plan/00-INVENTORY.md` 110 chức năng + `01-individual-cases.md` 106 TC-IND + `02-combo-scenarios.md` 12 TC-CMB + `REVIEW.md` bổ sung TC-IND-096..100/TC-CMB-006, chốt 30/06/2026 — thư mục này nằm ở **gốc workspace, NGOÀI `songchau-erp/`**). Đối chiếu mtime cho thấy `backend/app/api/v1/procurement_analytics.py` (module Xếp hạng NCC / Vendor Scorecard / Items History / Smart-Award) là mảng **hoàn toàn mới, không có trong 00-INVENTORY** (0 match "vendor-scorecard"/"RankDelta"). Bộ ca dưới đây bù đúng phần đó — không lặp lại bất kỳ TC-IND/TC-CMB nào.

> Ghi chú (đã sửa 03/07): thư mục `<workspace>/plans/bidding-e2e-test-plan/` **VẪN TỒN TẠI** — chỉ nằm ở **gốc workspace** (`C:\Users\ASUS\OneDrive\Documents\hệ thống song châu\plans\bidding-e2e-test-plan\`), KHÔNG nằm trong `songchau-erp/plans/` như các file E2E khác. Lần review trước tìm nhầm trong `songchau-erp/plans/` nên báo "không còn tồn tại" — đây là kết luận SAI, đã xác minh lại bằng `find`/`ls` trực tiếp trên workspace gốc. Bảng "map feature→ca" bên dưới tham chiếu ID của bộ đó (00-INVENTORY/01/02/REVIEW) để chứng minh KHÔNG-TRÙNG; có thể mở đọc lại nội dung thật tại đường dẫn trên bất cứ lúc nào.

## Dữ liệu chuẩn bị chung

**Tài khoản (dùng chung toàn bộ suite, không dùng tài khoản thật của Thang):**
- `test_admin@songchau.test`, `test_manager@songchau.test`, `test_procurement@songchau.test`, `test_staff@songchau.test` — 4 role trong `_READ_ROLES` (`procurement_analytics.py:61`), PHẢI xem được toàn bộ nhóm endpoint scorecard/items-history/smart-award.
- `test_sales@songchau.test`, `test_warehouse@songchau.test`, `test_accountant@songchau.test`, `test_viewer@songchau.test`, `test_director@songchau.test` — 5 role liền kề, PHẢI bị 403 trên cùng nhóm endpoint (role-matrix âm).
- 2 vendor account thuộc 2 NCC demo khác nhau trong DEMO-MIX-01 (`vendor1@demo.test`, `vendor2@demo.test`) — dùng cho ca chống-reuse-cổng-NCC (vendor gọi thẳng API scorecard/items-history bằng token cổng NCC phải bị chặn ở tầng route, không chỉ tầng role-string).

**Dữ liệu mồi (bổ sung trên nền DEMO-MIX-01 đã LIVE — 5 mã BQMS + 5 mã Nguồn cung + 3 NCC demo):**
- `DEMO-RANK-TIE`: 2 NCC demo có điểm scorecard hoà tuyệt đối (cùng `score`, khác `vendor_name`) trong cùng cửa sổ tháng — bắt hành vi share-rank + skip rank kế tiếp (`_ranks_from_factors`, `procurement_analytics.py:1007-1038`).
- `DEMO-RANK-NEWVENDOR`: 1 NCC demo mới hoàn toàn, KHÔNG có dữ liệu ở cửa sổ kỳ trước (prev window rỗng) — bắt `prev_rank=null` hiển thị "—", không crash toàn bảng.
- `DEMO-RANK-SPARSE`: 1 NCC demo có `invited < min_invites` (mặc định 3) — bắt `score=null`, `grade=null`, luôn rơi xuống đáy bảng (không sort như 0).
- `DEMO-ITEM-SUPERSEDED`: 1 mã hàng có award bị `superseded_by` (award cũ bị ghi đè bởi award mới) — bắt `/items/history` chỉ trả award ACTIVE.
- `DEMO-ITEM-MULTI-CCY`: 1 mã hàng được báo giá bởi ≥3 NCC ở ≥2 tiền tệ khác nhau (VND + USD) trong ≥2 phiên khác nhau (cross-batch) — bắt per-currency, cấm cộng gộp.
- `DEMO-ITEM-TARGET`: 1 `rfq_items.target_price` được set khác 0 trên mã hàng dùng cho `DEMO-ITEM-MULTI-CCY` — dùng làm "mồi rò rỉ" cho ca SEC-NEG (assert response KHÔNG có field `target_price` ở bất kỳ đâu, kể cả nested).
- `DEMO-SEALED-01`: 1 phiên đấu thầu sealed đang mở (`sealed_until` tương lai, chưa mở niêm phong) có ≥1 item đã có báo giá — dùng đối chiếu hành vi giữa Items tab (matrix, dùng `_sealed_active` gốc) và `smart-award` (bản inline `procurement_analytics.py:1644-1646`).
- KHÔNG đụng Samsung thật: mọi ca trong file này đọc dữ liệu nội bộ (scorecard/analytics), không có bước push/scrape nào — do đó **không có ca [MANUAL-SAMSUNG] trong file này**, toàn bộ là [AUTO-API] hoặc [SEMI-UI].
- Ca đụng prod dùng pattern **rollback-txn** (mở transaction gọi thẳng hàm/endpoint rồi rollback) — đúng pattern đã dùng cho imv-to-bidding (14/14) và secbqms-quote (14/14); prefix bản ghi tạo mới `DEMO-RANK-*` để dọn bằng glob nếu lỡ commit.

## Bảng test case

| Mã | Tên | Loại | Vai trò | Chuẩn bị | Các bước | Kỳ vọng | Ưu tiên | Tự động hoá |
|---|---|---|---|---|---|---|---|---|
| TC-DAUTHAU-001 | Mở trang Xếp hạng NCC — load thành công | Đơn lẻ | admin | Đăng nhập admin | 1. Mở `/analytics/vendor-scorecard`. 2. Chờ StatStrip load. | StatStrip hiện 5 số (NCC theo dõi / Đủ dữ liệu / Chưa đủ / Điểm TB / Hạng A) khớp response `GET /procurement/vendor-scorecard`; podium top-3 render; không lỗi console. | P1 | UI |
| TC-DAUTHAU-002 | Ma trận role — GET /procurement/vendor-scorecard | Negative | 9 role (bảng con) | 9 token test | Gọi `GET /procurement/vendor-scorecard?months=12` lần lượt với từng token. | admin/manager/procurement/staff → 200; sales/warehouse/accountant/viewer/director → 403. | P1 | API |
| TC-DAUTHAU-003 | Vendor token gọi thẳng API scorecard (chống reuse cổng NCC) | Negative | vendor | `vendor1@demo.test` token cổng NCC | Gọi `GET /procurement/vendor-scorecard` bằng Bearer token vendor. | 403 (role-string vendor không nằm trong `_READ_ROLES`); response KHÔNG rò bất kỳ field nào. | P1 | API |
| TC-DAUTHAU-004 | MonthsFilter — biên hợp lệ 1 và 36 | Đơn lẻ | procurement | Trang Xếp hạng NCC đã mở | 1. Đổi filter "Số tháng" = 1 → quan sát refetch. 2. Đổi = 36 → quan sát refetch. | Cả 2 giá trị trả 200, danh sách + Δ xếp hạng đổi theo cửa sổ mới (không giữ cache cũ). | P2 | UI |
| TC-DAUTHAU-005 | MonthsFilter — ngoài biên 0 và 37 | Negative | procurement | — | Gọi `GET /procurement/vendor-scorecard?months=0` và `?months=37`. | Cả 2 → 422 (Query ge=1, le=36). | P1 | API |
| TC-DAUTHAU-006 | Nút "Tải lại" — disabled khi đang fetch | Đơn lẻ | staff | Trang đã mở | 1. Bấm nút "Tải lại" (icon RotateCw). 2. Quan sát ngay lúc click. | Nút chuyển disabled + icon RotateCw→Loader2 trong lúc request chạy; sau khi xong bật lại, dữ liệu refresh. | P3 | UI |
| TC-DAUTHAU-007 | insufficient data — NCC dưới min_invites không ép điểm 0 | Đơn lẻ | procurement | Seed `DEMO-RANK-SPARSE` (invited=2 < min_invites=3) | Gọi `GET /procurement/vendor-scorecard?min_invites=3`, tìm NCC sparse trong response. | `score=null`, `grade=null` (KHÔNG phải 0/"C"); trên UI dòng này nằm cuối bảng, không có ở podium. | P1 | API+UI |
| TC-DAUTHAU-008 | min_invites — biên 1 và 50 | Đơn lẻ | procurement | — | Gọi `?min_invites=1` và `?min_invites=50`. | 200 cả 2; NCC ít lời mời hơn ngưỡng bị đánh insufficient tương ứng. | P2 | API |
| TC-DAUTHAU-009 | min_invites — ngoài biên 0 và 51 | Negative | procurement | — | Gọi `?min_invites=0` và `?min_invites=51`. | 422 cả 2 (Query ge=1, le=50). | P2 | API |
| TC-DAUTHAU-010 | prev_rank — NCC mới, kỳ trước không có dữ liệu | Đơn lẻ | procurement | Seed `DEMO-RANK-NEWVENDOR` | Gọi `GET /procurement/vendor-scorecard?months=1` (cửa sổ hẹp nhất để chắc chắn kỳ trước rỗng cho NCC mới). | `prev_rank=null` cho NCC mới; các NCC khác vẫn có `prev_rank` số nguyên bình thường (không bị null lây). Trên UI cột Δ hiện "—" đúng 1 dòng. | P1 | API+UI |
| TC-DAUTHAU-011 | BG-DAUTHAU-01: prev_rank — ép lỗi tính toán kỳ trước, xác nhận guard degrade | BUG-GATE | procurement | Tạo điều kiện khiến `_ranks_from_factors(prior_factors)` throw (vd. seed cửa sổ trước hoàn toàn không có `procurement_batches` nào — tháng đầu triển khai) | Gọi `GET /procurement/vendor-scorecard?months=36` trên tenant/khoảng thời gian không có dữ liệu trước đó. | **Kỳ vọng đúng**: response vẫn 200, toàn bộ `prev_rank=null` cho mọi NCC (degrade toàn phần theo `except Exception` dòng 1075-1077), KHÔNG 500. Ca FAIL nếu response 500 (ghi log kèm traceback) — không tính vào coverage tính năng cho tới khi xác nhận guard hoạt động đúng. | P1 | API |
| TC-DAUTHAU-012 | _ranks_from_factors — 2 NCC hoà điểm, share rank + skip kế tiếp | Đơn lẻ | procurement | Seed `DEMO-RANK-TIE` (2 NCC cùng score) | Gọi `GET /procurement/vendor-scorecard`, sort theo `score` DESC rồi `vendor_name` (không dấu, a→z). | 2 NCC hoà có cùng số `rank`; NCC thứ 3 (điểm thấp hơn) có `rank` = rank_hoà + 2 (bị skip 1 bậc, kiểu RANK() SQL) chứ không phải +1. | P1 | API |
| TC-DAUTHAU-013 | Thứ tự tie-break theo tên — kiểm chứng khớp UI | Đơn lẻ | procurement | `DEMO-RANK-TIE` | So sánh thứ tự 2 dòng hoà điểm trên bảng UI với response API. | Dòng có `vendor_name` xếp trước theo bảng chữ cái (localeCompare 'vi') hiện TRƯỚC trên UI — khớp đúng key sort của cả rank hiện tại lẫn rank kỳ trước. | P2 | UI |
| TC-DAUTHAU-014 | Cột Δ xếp hạng — có prev_rank, tính đúng chiều dương/âm | Đơn lẻ | procurement | 1 NCC tăng hạng, 1 NCC giảm hạng giữa 2 kỳ (dựng bằng cách chỉnh mốc `months`) | Đọc `RankDelta` trên 2 dòng NCC này. | `delta = prevRank - rank`; NCC tăng hạng (rank số nhỏ hơn kỳ trước) hiện mũi tên dương (▲), NCC giảm hạng hiện âm (▼), đúng dấu và độ lớn khớp phép trừ. | P2 | UI |
| TC-DAUTHAU-015 | Podium Top-3 — loại NCC insufficient | Đơn lẻ | procurement | `DEMO-RANK-SPARSE` có điểm giả định cao nếu KHÔNG bị loại | Mở trang, quan sát podium. | Podium chỉ liệt kê tối đa 3 NCC đã `scored` (không null); NCC sparse KHÔNG xuất hiện trong podium dù nếu tính điểm thô sẽ cao. | P1 | UI |
| TC-DAUTHAU-016 | Podium — rỗng khi <1 NCC đủ dữ liệu | Đơn lẻ | procurement | Filter `months=1` trên tập dữ liệu giả lập chỉ có NCC insufficient | Mở trang với `months=1`. | Podium hiển thị empty-state (không crash, không render 3 ô rỗng gây hiểu nhầm). | P2 | UI |
| TC-DAUTHAU-017 | Sort & phân nhóm — NCC insufficient luôn chìm đáy | Đơn lẻ | procurement | Trộn NCC scored điểm thấp (vd. 20/100) + `DEMO-RANK-SPARSE` | So sánh vị trí NCC sparse với NCC scored điểm 20/100 trên bảng. | NCC sparse luôn nằm SAU NCC scored điểm 20/100 dù về giá trị số `null` không nhỏ hơn 20 — xác nhận sort không coi null=0. | P1 | UI |
| TC-DAUTHAU-018 | ScoreCompositionBar — factor "quality" luôn ẩn (chưa implement) | Đơn lẻ | procurement | 1 NCC bất kỳ có đủ dữ liệu | Mở drawer chi tiết NCC, quan sát thanh phân rã điểm (ScoreCompositionBar). | Thanh có tối đa 5 phân đoạn (response/win/on_time/lead/price), KHÔNG có phân đoạn "quality" (factor null vĩnh viễn theo trọng số 0.15), tổng % các đoạn hiển thị = 100% sau renormalise. | P2 | UI |
| TC-DAUTHAU-019 | ScoreCompositionBar — factor thiếu dữ liệu bị loại khỏi thanh (không hiện 0%) | Đơn lẻ | procurement | NCC có `on_time_rate=null` do chưa có đơn giao đúng hạn/trễ nào | Mở drawer NCC này, quan sát ScoreCompositionBar. | Phân đoạn "on_time" biến mất khỏi thanh (width co lại phân bổ cho factor khác) — KHÔNG hiện dải 0% gây hiểu nhầm là NCC giao trễ 100%. | P2 | UI |
| TC-DAUTHAU-020 | Click dòng NCC — mở Drawer, tab mặc định = "Hiệu suất" | Đơn lẻ | procurement | Bảng đã load | 1. Click 1 dòng NCC bất kỳ. | Drawer mở, tab active mặc định = "Hiệu suất" (performance) — không phải "Hồ sơ DN" hay "Lịch sử". | P2 | UI |
| TC-DAUTHAU-021 | Tab "Hồ sơ DN" — lazy load, không fetch khi chưa active | Đơn lẻ | procurement | Drawer đang mở ở tab mặc định | 1. Mở DevTools Network. 2. Click tab "Hồ sơ DN". | Request `GET /procurement/vendor-scorecard/{id}/profile` CHỈ bắn ra khi click tab này (không bắn ngay lúc mở drawer); trả 200 hiển thị thông tin DN. | P2 | UI |
| TC-DAUTHAU-022 | Tab "Hiệu suất" — full scorecard engine đúng NCC đang chọn | Đơn lẻ | procurement | Drawer mở cho NCC X | Đọc dữ liệu tab Hiệu suất, đối chiếu `vendor_id` trong response `GET /procurement/vendor-scorecard/{vendor_id}` với NCC X đã click. | Dữ liệu hiển thị đúng NCC X (không lẫn NCC khác khi đổi dòng liên tiếp — click NCC Y ngay sau NCC X, verify tab tự cập nhật). | P2 | UI |
| TC-DAUTHAU-023 | Tab "Lịch sử" — lazy load 4 luồng độc lập | Đơn lẻ | procurement | Drawer mở, DevTools Network mở | 1. Click tab "Lịch sử". | Request `GET /procurement/vendor-scorecard/{id}/timeline` bắn đúng lúc click (không sớm hơn); 4 khối "Phiên đấu thầu / Hợp đồng / Đơn hàng / Đợt giao hàng" (batches/contracts/pos/deliveries) render riêng biệt. | P1 | UI |
| TC-DAUTHAU-024 | Tab "Lịch sử" — nút "Xem thêm" phân trang từng luồng độc lập | Đơn lẻ | procurement | NCC có >1 trang dữ liệu ở cả 4 luồng (seed đủ số dòng) | 1. Bấm "Xem thêm" ở khối "Đơn hàng" 2 lần liên tiếp. 2. Quan sát 3 khối còn lại. | Chỉ khối "Đơn hàng" tăng offset và nạp thêm dòng; 3 khối batches/contracts/deliveries GIỮ NGUYÊN offset/số dòng hiện tại (không bị fetch lại). | P1 | UI |
| TC-DAUTHAU-025 | Tab "Lịch sử" — mỗi khối collapsible, đóng/mở độc lập | Đơn lẻ | procurement | Tab Lịch sử đã load | 1. Đóng khối "Hợp đồng" (toggle). 2. Mở lại. | Đóng/mở không ảnh hưởng trạng thái offset/dữ liệu 3 khối kia; dữ liệu khối vừa mở lại vẫn còn (không fetch lại từ đầu, không mất offset đã "Xem thêm"). | P3 | UI |
| TC-DAUTHAU-026 | Tab "Lịch sử" — đa tiền tệ KHÔNG cộng gộp cross-currency | Đơn lẻ (Đa tiền tệ) | procurement | NCC có đơn hàng/hợp đồng cả VND lẫn USD trong `DEMO-MIX-01` mở rộng | Đọc tổng tiền hiển thị ở khối "Đơn hàng"/"Hợp đồng" cho NCC này. | KHÔNG có bất kỳ con số tổng nào cộng VND+USD chung 1 dòng; mỗi dòng lịch sử giữ nguyên currency gốc; nếu có subtotal theo currency thì tách riêng 2 dòng (VND: x, USD: y). | P1 | API+UI |
| TC-DAUTHAU-027 | GET /vendor-scorecard/{vendor_id} — ma trận role | Negative | 9 role | 1 vendor_id hợp lệ | Gọi endpoint với từng token. | admin/manager/procurement/staff → 200; 5 role còn lại → 403. | P1 | API |
| TC-DAUTHAU-028 | GET /vendor-scorecard/{vendor_id}/profile — vendor_id không tồn tại | Negative | procurement | vendor_id giả (999999) | Gọi endpoint. | 404 (không 500, không leak stack trace). | P2 | API |
| TC-DAUTHAU-029 | GET /items/history — thiếu cả item_code lẫn bqms_code | Negative | procurement | — | Gọi `GET /procurement/items/history` không kèm query param nào. | 422. | P1 | API |
| TC-DAUTHAU-030 | GET /items/history — chỉ item_code | Đơn lẻ | procurement | `DEMO-ITEM-MULTI-CCY` có item_code | Gọi `?item_code=<mã>`. | 200, trả 2 khối "awards" (Đã trúng) + "quotes" (Đã báo giá) đúng cấu trúc. | P1 | API |
| TC-DAUTHAU-031 | GET /items/history — chỉ bqms_code | Đơn lẻ | procurement | Mã hàng có cả item_code lẫn bqms_code | Gọi `?bqms_code=<mã>` (không kèm item_code). | 200, trả cùng kết quả logic như khi truy theo item_code tương ứng — xác nhận endpoint khớp mã theo cả 2 khoá. | P1 | API |
| TC-DAUTHAU-032 | GET /items/history — mã hàng chưa từng được báo giá | Đơn lẻ (empty-state) | procurement | 1 mã hàng mới tạo, chưa có RFQ nào | Gọi `?item_code=<mã mới>`. | 200, `awards=[]`, `quotes=[]` (không 404) — empty-state đúng nghĩa "chưa có lịch sử" chứ không phải lỗi. | P2 | API |
| TC-DAUTHAU-033 | GET /items/history — KHÔNG bao giờ trả target_price (SEC-NEG) | Negative | procurement, admin | `DEMO-ITEM-TARGET` có target_price != 0 | Gọi `?item_code=<mã DEMO-ITEM-TARGET>`, dump toàn bộ JSON response (đệ quy mọi field lồng nhau). | Response KHÔNG chứa field `target_price` ở BẤT KỲ cấp nào (top-level lẫn nested trong awards/quotes); assert bằng cách `"target_price" not in json.dumps(response)`. | P1 | API |
| TC-DAUTHAU-034 | GET /items/history — awards chỉ lấy active (superseded loại trừ) | Đơn lẻ | procurement | `DEMO-ITEM-SUPERSEDED` (award cũ có `superseded_by` trỏ tới award mới) | Gọi `?item_code=<mã DEMO-ITEM-SUPERSEDED>`. | Khối "awards" (Đã trúng) chỉ chứa award MỚI (superseded_by IS NULL); award cũ KHÔNG xuất hiện. | P1 | API |
| TC-DAUTHAU-035 | GET /items/history — per-currency, cấm cộng gộp | Đơn lẻ (Đa tiền tệ) | procurement | `DEMO-ITEM-MULTI-CCY` (≥3 NCC, ≥2 currency, cross-batch) | Gọi `?item_code=<mã>`, đọc từng dòng trong "quotes". | Mỗi dòng giữ nguyên `currency` gốc của báo giá đó; KHÔNG có field tổng hợp quy đổi 1 currency; số dòng = đúng số lượt báo giá thật (không gộp theo mã hàng). | P1 | API |
| TC-DAUTHAU-036 | GET /items/history — LIMIT 100 mỗi phần | Đơn lẻ (biên) | procurement | Mã hàng seed >100 lượt báo giá (hoặc mock DB trả >100 dòng nếu seed thật quá tốn) | Gọi `?item_code=<mã>`. | `len(quotes) <= 100`, `len(awards) <= 100`; nếu seed đúng 101 dòng, response chỉ trả 100, ORDER BY `submitted_at`/`awarded_at` DESC (100 dòng mới nhất). | P3 | API |
| TC-DAUTHAU-037 | GET /items/history — ma trận role | Negative | 9 role | — | Gọi endpoint với 9 token. | admin/manager/procurement/staff → 200; 5 role còn lại → 403; vendor token → 403 (chống reuse cổng NCC, xem TC-DAUTHAU-003). | P1 | API |
| TC-DAUTHAU-038 | Popover "Lịch sử mã hàng" trong tab Items — mở đúng dữ liệu | Đơn lẻ | admin | Mở 1 phiên đấu thầu có mã hàng `DEMO-ITEM-MULTI-CCY`, vào tab "Items" | 1. Bấm icon lịch sử (⏱) trên dòng mã hàng. 2. Chờ popover load. | Loading spinner hiện trong lúc chờ, sau đó 2 khối "Đã trúng" + "Đã báo giá" hiện tên NCC + giá (admin thấy được tên đối thủ, khác cổng NCC nơi ẩn); đóng popover bằng click ra ngoài — không còn hiện. | P1 | UI |
| TC-DAUTHAU-039 | Popover "Lịch sử mã hàng" — mã hàng chưa từng báo giá | Đơn lẻ (empty-state) | admin | Item mới thêm vào phiên, chưa NCC nào báo giá | Bấm icon lịch sử trên dòng mã hàng này. | Popover hiện thông báo rỗng ("Chưa có lịch sử báo giá cho mã này") — không spinner treo, không lỗi trắng trang. | P2 | UI |
| TC-DAUTHAU-040 | Popover "Lịch sử mã hàng" — lỗi mạng/API | Negative | admin | Giả lập API `/items/history` trả 500 (mock/intercept ở môi trường test) | Bấm icon lịch sử. | Popover hiện state lỗi đúng câu chữ "Không lấy được lịch sử mã hàng này" — không crash toàn trang Items. | P2 | UI |
| TC-DAUTHAU-041 | smart-award — đối chiếu sealed-consistency với matrix (items tab) trong CÙNG 1 phiên | Kết hợp | procurement | `DEMO-SEALED-01` (sealed_until tương lai, chưa mở niêm phong), đã có ≥1 báo giá | 1. Mở tab "Items" của phiên `DEMO-SEALED-01`, quan sát cột giá (matrix dùng `_sealed_active` gốc). 2. Gọi `GET /procurement/batches/{id}/smart-award` (dùng check inline dòng 1644-1646) cùng thời điểm. | Cả 2 nơi đều ẨN giá thật cho tới khi mở niêm phong (matrix hiện "🔒 Niêm phong" hoặc tương đương; smart-award trả lỗi/từ chối gợi ý do sealed) — hai định nghĩa "sealed" phải đồng nhất về kết quả trong cùng 1 phiên. Nếu 1 trong 2 nơi lộ giá còn nơi kia không → FAIL nghiêm trọng (lệch định nghĩa sealed). | P1 | API+UI |
| TC-DAUTHAU-042 | smart-award — ma trận role | Negative | 9 role | `DEMO-SEALED-01` đã mở niêm phong (hết sealed_until) | Gọi endpoint với 9 token. | admin/manager/procurement/staff → 200; 5 role còn lại → 403. (Đã có TC-IND liên quan theo ghi chú brainstorm — ca này CHỈ bổ sung riêng phần đối chiếu sealed ở TC-DAUTHAU-041, không lặp coverage role cơ bản đã có.) | P3 | API |
| TC-DAUTHAU-043 | Route-guard FE — role không đủ quyền truy cập URL trực tiếp | Negative | sales, viewer | Đăng nhập bằng `test_sales@songchau.test` | 1. Gõ thẳng URL `/analytics/vendor-scorecard` vào trình duyệt (không qua menu, vì trang này không có trong sidebar/permission mapping đã audit). | FE phải chặn (redirect về trang chủ / hiện "Không có quyền truy cập") — KHÔNG được render bảng dù response API 403 (nếu FE chỉ dựa vào lỗi API để ẩn UI mà không có guard riêng, ghi nhận là gap cần vá). | P1 | UI |
| TC-DAUTHAU-044 | Route-guard FE — vai trò được phép nhưng KHÔNG có menu — vẫn vào được bằng URL | Đơn lẻ | staff | Đăng nhập `test_staff@songchau.test` | 1. Xác nhận menu sidebar KHÔNG có mục "Xếp hạng NCC" (theo ghi chú audit trước). 2. Gõ thẳng URL. | Trang vẫn load bình thường (staff nằm trong `_READ_ROLES` nên BE cho phép) — xác nhận đây là gap UX (thiếu điều hướng) chứ không phải lỗi bảo mật; ghi nhận đề xuất bổ sung menu item cho Thang quyết định. | P2 | UI |
| TC-DAUTHAU-045 | DEMO-MIX-01 mở rộng — twin etl+onedrive không làm lệch số liệu scorecard | Kết hợp (Dedup) | procurement | Seed 1 cặp twin etl+onedrive_sync cho 1 mã trong `DEMO-MIX-01` | 1. Gọi `GET /procurement/vendor-scorecard`. 2. Đối chiếu số lượt mời/báo giá của NCC liên quan mã này với số thực tế (đếm tay theo `bqms_dedup` CTE quy ước — DISTINCT ON ordered by `quote_unlocked` DESC + `bqms_push_status` DESC). | Số liệu KHÔNG bị đếm đôi do twin — nếu response đếm ra số gấp đôi thực tế → FAIL (bug đếm đôi giống rủi ro đã ghi trong memory `reference_bqms_rfq_dup_rows`). | P1 | API |
| TC-DAUTHAU-046 | Đổi `months` giữa 2 request liên tiếp — Δ xếp hạng đổi theo cửa sổ, không dính cache cũ | Kết hợp | procurement | Trang đã mở với `months=12` | 1. Ghi lại `prev_rank` của 1 NCC ở `months=12`. 2. Đổi filter sang `months=6`. 3. Ghi lại `prev_rank` cùng NCC. | 2 giá trị `prev_rank` khác nhau (vì cửa sổ trước dịch theo `months`, theo comment dòng 1061-1067) — nếu giống hệt nhau ở NCC có dữ liệu thay đổi theo thời gian, nghi ngờ cache/stale state ở FE. | P2 | API+UI |
| TC-DAUTHAU-047 | Rollback-txn — xác nhận 0 rác sau khi chạy toàn bộ ca AUTO-API nhóm scorecard | Negative (Teardown) | admin | Toàn bộ TC-DAUTHAU-001..046 đã chạy trên prod theo pattern rollback-txn | Sau khi chạy, kiểm tra không còn bản ghi `DEMO-RANK-*`/`DEMO-ITEM-*` mới nào tồn tại ngoài transaction (query trực tiếp DB, không qua API). | 0 dòng rác — mọi INSERT phục vụ test đã rollback; nếu có seed bắt buộc phải commit (vd. DEMO-MIX-01 mở rộng), phải nằm trong danh sách dọn dẹp có prefix `DEMO-` để glob xoá. | P1 | Tay |

## Map feature→ca (chứng minh phủ 100%)

| Feature | Ca phủ | Ghi chú KHÔNG-TRÙNG với bộ 118/122 ca cũ |
|---|---|---|
| F-RANK-01 (Trang Xếp hạng NCC) | TC-DAUTHAU-001, 043, 044 | Trang hoàn toàn mới, 0 match trong 00-INVENTORY |
| F-RANK-02 (MonthsFilter) | TC-DAUTHAU-004, 005, 046 | — |
| F-RANK-03 (Nút Tải lại) | TC-DAUTHAU-006 | — |
| F-RANK-04 (GET /vendor-scorecard) | TC-DAUTHAU-002, 003, 007, 008, 009, 045 | Role-matrix riêng cho endpoint này, khác role-matrix đã có ở TC-IND cho `/batches`, `/rfq` |
| F-RANK-05 (_ranks_from_factors tie) | TC-DAUTHAU-012, 013 | Thuật toán rank hoàn toàn mới, chưa ai test |
| F-RANK-06 (Cột Δ xếp hạng) | TC-DAUTHAU-010, 011 (BG-DAUTHAU-01), 014 | — |
| F-RANK-07 (Podium Top-3) | TC-DAUTHAU-015, 016 | — |
| F-RANK-08 (Sort scored/sparse) | TC-DAUTHAU-017 | — |
| F-RANK-09 (ScoreCompositionBar) | TC-DAUTHAU-018, 019 | — |
| F-RANK-10 (Click mở Drawer) | TC-DAUTHAU-020 | — |
| F-RANK-11 (Tab Hồ sơ DN) | TC-DAUTHAU-021, 028 | Endpoint `/profile` chưa có TC nào tham chiếu trước đây |
| F-RANK-12 (Tab Hiệu suất) | TC-DAUTHAU-022 | — |
| F-RANK-13 (Tab Lịch sử 4 luồng) | TC-DAUTHAU-023, 024, 025, 026 | 4-stream pagination độc lập + đa tiền tệ chưa có TC nào |
| F-RANK-14 (GET /items/history) | TC-DAUTHAU-029..037 | REVIEW.md ghi path `/batches/{id}/items/{itemId}/history` — code thật dùng query-param `/items/history?item_code=&bqms_code=` (`procurement_analytics.py:1518`); TC-IND-096..100 cũ KHÔNG khớp path thật → coi như CHƯA phủ, viết lại đầy đủ ở đây |
| F-RANK-15 (Popover Items tab) | TC-DAUTHAU-038, 039, 040 | Nút con của F004 (ItemsTable) trong 00-INVENTORY nhưng popover riêng chưa có TC |
| F-RANK-16 (smart-award sealed-consistency) | TC-DAUTHAU-041, 042 | Role-matrix cơ bản của smart-award ĐÃ có (TC-IND theo ghi chú F011, dòng 793/1431/1746 của 01-individual-cases.md) — ca 041 CHỈ bổ sung phần đối chiếu 2 định nghĩa "sealed" (matrix vs inline), không lặp coverage cũ |
| SEC-NEG target_price (rủi ro đã ghi) | TC-DAUTHAU-033 | Bổ sung theo risk "BLOCKER bảo mật SEC-001 style" — 5 endpoint đều phải test, ca này phủ `/items/history`; 4 endpoint scorecard còn lại phủ gián tiếp qua TC-DAUTHAU-002/027/028 (role-matrix) — **CHƯA CÓ ca SEC-NEG target_price riêng cho `/vendor-scorecard`, `/vendor-scorecard/{id}`, `/vendor-scorecard/{id}/profile`, `/vendor-scorecard/{id}/timeline`** → xem "Việc còn tồn" bên dưới |
| Dedup twin (data_prep_strategy) | TC-DAUTHAU-045 | — |
| Rollback-txn teardown | TC-DAUTHAU-047 | — |

### Việc còn tồn (ghi nhận minh bạch, không tự ý mở rộng phạm vi)

1. **SEC-NEG target_price chưa phủ đủ 5/5 endpoint** — TC-DAUTHAU-033 mới phủ `/items/history`. Cần bổ sung 4 ca tương tự (dump JSON đệ quy, assert `"target_price" not in json.dumps(...)`) cho `GET /vendor-scorecard`, `/vendor-scorecard/{id}`, `/vendor-scorecard/{id}/profile`, `/vendor-scorecard/{id}/timeline` — đánh số tiếp TC-DAUTHAU-048..051 ở đợt sau nếu Thang duyệt mở rộng.
2. Các mảng "sót" khác do Plan lần 2 phát hiện (AUTH/TOKEN-REVOCATION, NOTIFICATIONS, EXCHANGE_RATES, HR leave/attendance/kpi, IMV bề mặt riêng, WORKFLOWS engine, SUPPLIERS+PURCHASE_ORDERS, DASHBOARD, AUDIT-IMMUTABLE trigger, GLOBAL SEARCH, refresh-giữa-wizard, PWA/SYSTEM_HEALTH/RETRY_QUEUE) đều **NGOÀI phạm vi "Đấu thầu NCC + cổng vendor"** của file này — cần file E2E riêng theo từng mảng (AUTH.md, NOTIF.md, FX.md, HR.md...) như quy ước numbering đã thống nhất, không trộn vào đây.
